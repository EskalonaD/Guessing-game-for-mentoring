import { GameAnchorDirective } from './game-anchor.directive';

describe('GameAnchorDirective', () => {
  it('should create an instance', () => {
    // const directive = new GameAnchorDirective(a);
    // expect(directive).toBeTruthy();
  });
});
